; This file is for local development — the CI workflow uses NODE_AUTH_TOKEN directly.
; DO NOT commit real tokens here.

; Usage for local `npm publish`:
;   export NPM_TOKEN=npm_xxxxxxxxxxxx
;   npm publish

//registry.npmjs.org/:_authToken=${NPM_TOKEN}
